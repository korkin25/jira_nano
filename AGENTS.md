# AGENTS.md

**Canonical rules live in [`CLAUDE.md`](CLAUDE.md).** Read that file first — it
defines what the project is, the strict language rules, the autonomous
development workflow, and the conventions. This file is only a pointer.

- **One source of rules:** do not duplicate policy here; follow `CLAUDE.md`.
- **Skill target:** `jira_nano` targets the **Agent Skills** standard
  ([agentskills.io](https://agentskills.io)) for portability, so it can be
  packaged as a `SKILL.md` and consumed by OpenClaw / Claude / other agents.
- **Language:** all repository content is English; live chat with the user is
  Russian (see `CLAUDE.md`).
