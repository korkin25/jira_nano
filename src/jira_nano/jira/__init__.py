"""Jira-compatibility presentation layer (shared by the MCP and HTTP adapters).

- ``mapper`` — Ticket <-> Jira issue JSON (v2 / v3) (JN-33)
- ``jql``    — the JQL subset parser (JN-30)
- ``adf``    — Markdown <-> ADF conversion (JN-31)
"""
