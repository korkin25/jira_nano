"""jira_nano — a lightweight, AI-native, git-backed issue tracker.

Git is the single source of truth; the SQLite cache and working files are local
caches for speed. See ``docs/architecture.md`` for the design and ``TODO.md`` for
the phased task plan.
"""
from __future__ import annotations

# Placeholder — release.yml injects the real version from GitVersion at publish time.
__version__ = "0.0.0"
